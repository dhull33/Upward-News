import React, { Component } from 'react'
import { Card, CardImg, CardTitle, CardText, CardGroup, CardSubtitle, CardBody } from 'reactstrap'

class marketNews extends Component{
  render(){

    let marketItems
    if(this.props.marketNews){
      marketItems
    }

  }
}